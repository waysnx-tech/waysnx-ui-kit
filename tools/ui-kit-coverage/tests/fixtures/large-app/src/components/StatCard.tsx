// Project-defined component; wraps UI Kit (its file imports @waysnx).
import { Button } from "@waysnx/ui-core";

export function StatCard() {
  return (
    <div className="stat-card">
      <Button>Details</Button>
    </div>
  );
}
