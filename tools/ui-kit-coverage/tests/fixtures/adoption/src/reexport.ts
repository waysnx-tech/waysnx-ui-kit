// Re-export from ui-layout. Tracked separately; must NOT count as direct usage (spec §7).
// ui-layout is declared but only re-exported here → imported:false, used:false.
export { Grid } from "@waysnx/ui-layout";
